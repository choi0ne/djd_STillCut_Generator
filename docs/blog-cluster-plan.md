# 📦 블로그 클러스터화 계획서

> 작성일: 2025-12-30
> 목적: Stage 0.5에서 시리즈물 추천 기준 신설, 블로그 글 클러스터화 강화

---

## 🎯 핵심 목표

**단일 블로그 글 → 4개 시리즈 클러스터**로 확장하여:
- SEO 내부 링크 강화
- 독자 체류 시간 증가
- 주제 권위(Topic Authority) 구축
- 콘텐츠 생산 효율화

---

## 📊 Stage 0.5 개선: 시리즈 추천 점수 추가

### 현재 스코어링 (4대 축)

| 축 | 설명 | 배점 |
|----|------|------|
| 검색량 | 월간 검색량 추정 | 25점 |
| 경쟁도 | 상위 노출 난이도 | 25점 |
| 전환성 | 환자 내원 연결 가능성 | 25점 |
| 계절성 | 시즌/트렌드 적합도 | 25점 |
| **합계** | | **100점** |

### 개선된 스코어링 (5대 축)

| 축 | 설명 | 배점 |
|----|------|------|
| 검색량 | 월간 검색량 추정 | 20점 |
| 경쟁도 | 상위 노출 난이도 | 20점 |
| 전환성 | 환자 내원 연결 가능성 | 20점 |
| 계절성 | 시즌/트렌드 적합도 | 15점 |
| **⭐ 클러스터 잠재력** | 시리즈 확장 가능성 | **25점** |
| **합계** | | **100점** |

### 클러스터 잠재력 평가 기준

| 점수 | 기준 |
|------|------|
| 25점 | 4개 이상 연관 주제 즉시 도출 가능 |
| 20점 | 3개 연관 주제 도출 가능 |
| 15점 | 2개 연관 주제 도출 가능 |
| 10점 | 1개 연관 주제만 도출 가능 |
| 5점 | 단독 주제 (확장 어려움) |

---

## 🔄 시리즈 클러스터 생성 플로우

### 화면 구성

```
┌─────────────────────────────────────────────────────────────────────┐
│  Stage 0.5: 주제 스코어링                                           │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  📊 점수순 주제 목록 (클릭하면 하단에 시리즈 표시)                   │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │ 1. 공황장애 초기증상                    92점 📦×4  [클릭] ◀── │ │
│  │ 2. 불면증 자연치료법                    87점 📦×4             │ │
│  │ 3. 어지러움 원인                        83점 📦×3             │ │
│  │ 4. 소화불량 관리법                      79점 📦×4             │ │
│  └────────────────────────────────────────────────────────────────┘ │
│                                                                      │
│  ─────────────────────────────────────────────────────────────────  │
│                                                                      │
│  📦 시리즈 클러스터 (선택된 주제의 4개 글) ⬇️ 자동 생성             │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │ 🎯 [메인] 공황장애 초기증상 - 이런 증상이면 의심하세요          │ │
│  │      ▶️ 1-7단계 시작                                           │ │
│  ├────────────────────────────────────────────────────────────────┤ │
│  │ 🔍 [세부] 공황장애 자가 진단법 - 체크리스트 10가지              │ │
│  │      ▶️ 1-7단계 시작                                           │ │
│  ├────────────────────────────────────────────────────────────────┤ │
│  │ ↔️ [확장] 직장인 출근길 공황 대처법                             │ │
│  │      ▶️ 1-7단계 시작                                           │ │
│  ├────────────────────────────────────────────────────────────────┤ │
│  │ ➡️ [후속] 공황장애 약 없이 관리 가능할까?                       │ │
│  │      ▶️ 1-7단계 시작                                           │ │
│  └────────────────────────────────────────────────────────────────┘ │
│                                                                      │
│  [📦 시리즈 4개 모두 일괄 생성]                                     │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### 상호작용 플로우

```
1. AI가 5대 축 기준으로 점수 부여
   - 검색량, 경쟁도, 전환성, 계절성, 클러스터 잠재력
        │
        ▼
2. 점수순으로 주제 목록 나열
   - 높은 점수 순서대로 표시
   - 각 주제에 📦×N 표시 (확장 가능 시리즈 개수)
        │
        ▼
3. 사용자가 주제 클릭
        │
        ▼
4. ⭐ 하단에 4개 시리즈 자동 표시
   - 메인 / 세부 / 확장 / 후속
   - 각각 "▶️ 1-7단계 시작" 버튼
        │
        ▼
5. 원하는 시리즈 선택 → 1-7단계 진행
   또는
   "📦 시리즈 4개 모두 일괄 생성" 클릭
```


## 📝 시리즈 유형 정의

### 4가지 시리즈 유형

| 유형 | 설명 | 예시 (메인: 공황장애 초기증상) |
|------|------|-------------------------------|
| **메인 (Main)** | 선택된 핵심 주제 | 공황장애 초기증상 - 이런 증상이면 의심하세요 |
| **세부 (Drill-down)** | 더 구체적인 하위 주제 | 공황장애 자가 진단법 - 체크리스트 10가지 |
| **확장 (Lateral)** | 관련 상황/타겟 확장 | 직장인 출근길 공황 대처법 |
| **후속 (Follow-up)** | 다음 단계/치료/관리 | 공황장애 약 없이 관리 가능할까? |

### 시리즈 유형별 특징

```
[메인 Main]
- 검색량 가장 높은 핵심 키워드
- 시리즈의 허브(Hub) 역할
- 다른 3개 글로 내부 링크

[세부 Drill-down]
- 메인 주제의 세부 사항
- 롱테일 키워드 타겟
- 메인 글에서 "자세히 알아보기" 링크

[확장 Lateral]
- 다른 타겟/상황으로 확장
- 새로운 독자층 유입
- 메인 글에서 "관련 글" 링크

[후속 Follow-up]
- 독자의 다음 질문 예측
- 치료/관리/예방 등
- 메인 글에서 "다음 단계" 링크
```

---

## 🛠️ 기술 구현

### 1. 데이터 구조

```typescript
interface ScoredTopic {
    title: string;
    score: number;
    summary: string;
    
    // ⭐ 클러스터 관련 필드 추가
    clusterScore: number;           // 클러스터 잠재력 점수 (0-25)
    clusterSuggestions?: ClusterSuggestion[];  // AI 생성 시리즈 제안
}

interface ClusterSuggestion {
    type: 'main' | 'drill-down' | 'lateral' | 'follow-up';
    title: string;
    reason: string;
    estimatedSearchVolume: 'high' | 'medium' | 'low';
    priority: number;  // 1-4 (1이 가장 높음)
}

interface StageData {
    // ... 기존 필드
    
    // ⭐ 클러스터 관련 필드 추가
    selectedCluster?: {
        main: string;
        series: ClusterSuggestion[];
    };
}
```

### 2. AI 프롬프트 (Stage 0.5 확장)

```typescript
const CLUSTER_SCORING_PROMPT = `
## Stage 0.5: 주제 스코어링 + 클러스터 분석

주제 후보들을 5대 축으로 평가하고, 시리즈 클러스터를 제안하세요.

### 평가 축 (100점 만점)
1. 검색량 (20점): 월간 검색량 추정
2. 경쟁도 (20점): 상위 노출 난이도 (낮을수록 좋음)
3. 전환성 (20점): 환자 내원 연결 가능성
4. 계절성 (15점): 현재 시즌/트렌드 적합도
5. 클러스터 잠재력 (25점): 시리즈 확장 가능성

### 클러스터 제안 규칙
- 각 주제에 대해 4개 시리즈 제안
- 유형: main, drill-down, lateral, follow-up
- 각 시리즈 글의 예상 제목과 이유 제시

### 출력 형식 (JSON)
[
    {
        "title": "공황장애 초기증상",
        "score": 92,
        "summary": "검색량 높고 시리즈 확장성 우수",
        "breakdown": {
            "searchVolume": 18,
            "competition": 16,
            "conversion": 20,
            "seasonality": 13,
            "clusterPotential": 25
        },
        "clusterSuggestions": [
            {
                "type": "main",
                "title": "공황장애 초기증상 - 이런 증상이면 의심하세요",
                "reason": "핵심 키워드, 시리즈 허브 역할",
                "estimatedSearchVolume": "high",
                "priority": 1
            },
            {
                "type": "drill-down",
                "title": "공황장애 자가 진단법 - 체크리스트 10가지",
                "reason": "초기증상 확인 후 자연스러운 다음 질문",
                "estimatedSearchVolume": "medium",
                "priority": 2
            },
            {
                "type": "lateral",
                "title": "직장인 출근길 공황 대처법",
                "reason": "타겟 독자(직장인) 상황 확장",
                "estimatedSearchVolume": "medium",
                "priority": 3
            },
            {
                "type": "follow-up",
                "title": "공황장애 약 없이 관리 가능할까?",
                "reason": "약물 거부감 있는 환자 대응, 한방 치료 연결",
                "estimatedSearchVolume": "high",
                "priority": 4
            }
        ]
    }
]
`;
```

### 3. UI 구현 (Stage 0.5 확장)

```tsx
// components/BlogWriterEditor.tsx - Stage 0.5 UI 확장

// 주제 선택 시 클러스터 표시
const renderTopicCard = (topic: ScoredTopic, index: number) => (
    <div 
        className={`topic-card ${selectedTopicIndex === index ? 'selected' : ''}`}
        onClick={() => handleSelectTopic(index)}
    >
        <div className="topic-header">
            <h3>{topic.title}</h3>
            <span className="score-badge">{topic.score}점</span>
        </div>
        
        <div className="score-breakdown">
            <div>검색량: {topic.breakdown.searchVolume}/20</div>
            <div>경쟁도: {topic.breakdown.competition}/20</div>
            <div>전환성: {topic.breakdown.conversion}/20</div>
            <div>계절성: {topic.breakdown.seasonality}/15</div>
            <div className="cluster-score">
                📦 클러스터: {topic.breakdown.clusterPotential}/25
            </div>
        </div>
        
        {/* ⭐ 클러스터 시리즈 미리보기 */}
        {topic.clusterSuggestions && (
            <div className="cluster-preview">
                <h4>📦 시리즈 클러스터 (4개)</h4>
                {topic.clusterSuggestions.map((s, i) => (
                    <div key={i} className={`series-item ${s.type}`}>
                        <span className="type-badge">{getTypeBadge(s.type)}</span>
                        <span className="series-title">{s.title}</span>
                    </div>
                ))}
            </div>
        )}
    </div>
);

const getTypeBadge = (type: string) => {
    switch(type) {
        case 'main': return '🎯 메인';
        case 'drill-down': return '🔍 세부';
        case 'lateral': return '↔️ 확장';
        case 'follow-up': return '➡️ 후속';
        default: return '';
    }
};
```

### 4. 시리즈 개별 선택 → 1-7단계 전체 진행

> ⭐ **핵심: 시리즈 4개 중 원하는 글을 선택해서 1-7단계 전체 진행 가능**

```
📦 시리즈 클러스터
├── 🎯 [메인] 공황장애 초기증상 ────────── [선택] → 1-7단계 진행
├── 🔍 [세부] 공황장애 자가 진단법 ──────── [선택] → 1-7단계 진행
├── ↔️ [확장] 직장인 출근길 공황 대처법 ── [선택] → 1-7단계 진행
└── ➡️ [후속] 공황장애 약 없이 관리 가능할까? [선택] → 1-7단계 진행
```

#### UI 구현: 시리즈 선택 패널

```tsx
// 시리즈 클러스터 선택 UI
const renderClusterSeries = (cluster: ClusterSuggestion[]) => (
    <div className="cluster-series-panel">
        <h3>📦 시리즈 클러스터</h3>
        <p className="hint">원하는 글을 선택하면 1-7단계 전체 진행됩니다</p>
        
        {cluster.map((series, index) => (
            <div 
                key={index}
                className={`series-card ${selectedSeriesIndex === index ? 'selected' : ''}`}
                onClick={() => setSelectedSeriesIndex(index)}
            >
                <div className="series-header">
                    <span className="type-badge">{getTypeBadge(series.type)}</span>
                    <span className="series-title">{series.title}</span>
                </div>
                <div className="series-meta">
                    <span>검색량: {series.estimatedSearchVolume}</span>
                    <span>우선순위: {series.priority}/4</span>
                </div>
                <p className="series-reason">{series.reason}</p>
                
                {/* ⭐ 개별 진행 버튼 */}
                <button 
                    className="start-series-btn"
                    onClick={(e) => {
                        e.stopPropagation();
                        handleStartSeries(series);
                    }}
                >
                    ▶️ 이 글로 1-7단계 시작
                </button>
            </div>
        ))}
        
        {/* 일괄 생성 버튼 */}
        <button 
            className="batch-all-btn"
            onClick={handleBatchSeriesGeneration}
        >
            📦 시리즈 4개 모두 일괄 생성
        </button>
    </div>
);
```

#### 개별 시리즈 진행 로직

```typescript
// 시리즈 중 하나 선택해서 1-7단계 전체 진행
const handleStartSeries = (series: ClusterSuggestion) => {
    // 1. 선택된 시리즈를 selectedTopic으로 설정
    setStageData(prev => ({
        ...prev,
        selectedTopic: series.title,
        // 시리즈 정보 보존 (나중에 내부 링크 생성용)
        currentSeriesContext: {
            type: series.type,
            cluster: selectedCluster
        }
    }));
    
    // 2. Stage 1로 이동 (키워드 클러스터)
    setCurrentStage(1);
    
    // 3. 자동 실행 또는 수동 진행 선택
    if (autoProceed) {
        handleBatchProcess();  // 1-6단계 자동 실행
    }
};

// 1-7단계 완료 후 다음 시리즈 진행 안내
const handleSeriesComplete = () => {
    const currentContext = stageData.currentSeriesContext;
    const remainingSeries = currentContext.cluster.filter(
        s => s.title !== stageData.selectedTopic
    );
    
    if (remainingSeries.length > 0) {
        // 다음 시리즈 진행 안내
        showModal({
            title: '✅ 시리즈 글 완료!',
            content: `
                "${stageData.selectedTopic}" 완료!
                
                📦 남은 시리즈:
                ${remainingSeries.map(s => `- ${s.title}`).join('\n')}
                
                다음 시리즈를 진행하시겠습니까?
            `,
            actions: [
                { label: '다음 시리즈 시작', onClick: () => handleStartSeries(remainingSeries[0]) },
                { label: '나중에', onClick: closeModal }
            ]
        });
    }
};
```

#### 시리즈 진행 상태 표시

```tsx
// 시리즈 진행 상태 표시 (상단 배너)
const SeriesProgressBanner = () => {
    if (!stageData.currentSeriesContext) return null;
    
    const { cluster } = stageData.currentSeriesContext;
    const currentIndex = cluster.findIndex(s => s.title === stageData.selectedTopic);
    
    return (
        <div className="series-progress-banner">
            <span>📦 시리즈 진행 중: {currentIndex + 1}/4</span>
            <div className="series-dots">
                {cluster.map((s, i) => (
                    <span 
                        key={i} 
                        className={`dot ${i === currentIndex ? 'active' : ''} ${i < currentIndex ? 'done' : ''}`}
                        title={s.title}
                    />
                ))}
            </div>
        </div>
    );
};
```

### 5. 시리즈 일괄 생성 (4개 전부)

```tsx
// 시리즈 4개 일괄 생성 버튼
const handleBatchSeriesGeneration = async () => {
    if (!selectedCluster) return;
    
    setIsBatchProcessing(true);
    setBatchProgress({ current: 0, total: 4 });
    
    for (let i = 0; i < selectedCluster.series.length; i++) {
        const series = selectedCluster.series[i];
        
        // 현재 시리즈 설정
        setStageData(prev => ({
            ...prev,
            selectedTopic: series.title,
            currentSeriesContext: { type: series.type, cluster: selectedCluster }
        }));
        
        // 1-7단계 자동 실행
        await runAllStages(series.title);
        
        // 진행 상태 업데이트
        setBatchProgress({ current: i + 1, total: 4 });
    }
    
    setIsBatchProcessing(false);
    showToast('📦 시리즈 4개 생성 완료!');
};

// UI
<button 
    onClick={handleBatchSeriesGeneration}
    className="batch-series-btn"
    disabled={isBatchProcessing}
>
    {isBatchProcessing 
        ? `⏳ 진행 중... (${batchProgress.current}/${batchProgress.total})`
        : '📦 시리즈 4개 일괄 생성'
    }
</button>
```


---

## 🔗 내부 링크 자동 생성

### 시리즈 글 간 상호 링크

```typescript
// 시리즈 글 완성 시 상호 링크 자동 삽입
const insertSeriesLinks = (content: string, cluster: ClusterSuggestion[]) => {
    const linksSection = `
---

## 📌 같이 보시면 좋은 글

${cluster
    .filter(s => s.type !== 'main')
    .map(s => `- [${s.title}](/${slugify(s.title)})`)
    .join('\n')}
`;
    
    return content + linksSection;
};
```

---

## 📈 기대 효과

| 지표 | 현재 | 개선 후 |
|------|------|---------|
| 글당 내부 링크 | 0-1개 | 3-4개 |
| 독자 체류 시간 | 2분 | 5분+ |
| 주제 권위 | 단일 글 | 4개 클러스터 |
| 콘텐츠 생산 | 주제별 개별 기획 | 시리즈 자동 제안 |

---

## ✅ 체크리스트

- [x] `ScoredTopic` 인터페이스에 `clusterSuggestions` 필드 추가 ✅
- [x] `ClusterSuggestion` 인터페이스 정의 ✅
- [x] `StageData`에 `selectedCluster`, `currentSeriesContext` 필드 추가 ✅
- [x] Stage 0.5 AI 프롬프트에 클러스터 분석 추가 ✅
- [x] 주제 카드 UI에 클러스터 시리즈 미리보기 추가 ✅
- [x] 시리즈 개별 선택 → 1-7단계 진행 버튼 구현 ✅
- [x] 시리즈 진행 배너 (상단) 구현 ✅
- [x] "시리즈 첫 번째 글(메인)부터 시작" 버튼 구현 ✅
- [x] Stage 6 프롬프트에 "같이 읽으면 좋은 글" 섹션 자동 추가 ✅
- [ ] 테스트: 시리즈 생성 및 "같이 읽으면 좋은 글" 섹션 확인

